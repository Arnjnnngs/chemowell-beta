# CLAUDE.md — care-tracker STAGING (repo currently named `chemowell-beta`)

**THIS FILE MUST BE CALLED `CLAUDE.md`. The instructions live in `claude/care-tracker-staging.md`.**

Claude Code auto-loads a file named exactly `CLAUDE.md` and nothing else. This repo's rules used to
live in `BETA_CLAUDE.md` and were therefore never read by anything. The app-specific name is on the
imported file; this stub exists only so the loader finds it.

**READ THE FIRST SECTION OF THE IMPORTED FILE BEFORE ANYTHING ELSE.** Despite the repo name, this is
NOT the ChemoWell product — it is `care-tracker`'s staging copy, one specific person's app, and her
name belongs in it. The product is `chemowell-app-beta`.

@./claude/care-tracker-staging.md
